// Simplest rendering effect: fill entire image with solid color
init:
  meta[1] = random(256); // R
  meta[2] = random(256); // G
  meta[3] = random(256); // B
pixel:
  *img++ = meta[1]; *img++ = meta[2]; *img++ = meta[3];

# vi:syntax=cpp
