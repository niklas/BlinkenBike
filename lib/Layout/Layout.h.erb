#ifndef __LAYOUT_H__
#define __LAYOUT_H__
#include <Arduino.h>
#define FLOOR_PIXEL_COUNT <%= floor_tube.led_count %>
byte floorMap(byte);

enum Orientation {
  ORIENTATION_LINEAR = 0,
  ORIENTATION_FLOOR = 1,


  ORIENTATION_NIL = -1
};

#define NUM_ORIENTATIONS 2

#endif
// vi:syntax=cpp
